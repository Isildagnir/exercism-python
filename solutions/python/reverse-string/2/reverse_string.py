"function to reverse a string"

def reverse(text):
    """reverse a given string i.e. reading them from right to left.

    :param text: str - the given string.
    :return: str - the given string reversed.
    """
    return "".join(reversed(text))
